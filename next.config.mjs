const nextConfig = {
  experimental: {
    missingSuspenseWithCSRBailout: false,
  },
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  // output: 'export',

};

export default nextConfig;

