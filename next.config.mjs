const nextConfig = {
  experimental: {
    missingSuspenseWithCSRBailout: false,
  },
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  // output: 'export',
  // assetPrefix: 'https://uniskai.id'

};

export default nextConfig;

