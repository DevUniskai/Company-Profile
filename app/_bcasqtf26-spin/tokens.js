/** Shared look of the spin game. */

// Circular Std — registered with real weights in styles/font.js
export const CIRCULAR = "'CircularStd', sans-serif";

// the orange the display type sits in
export const ACCENT = "#d3774e";

export const BACKGROUND_IMAGE = "/static/images/sqtf26/assets/BACKGROUND.jpg";

export const CONFETTI_COLORS = [
  "#bf5432",
  "#ef9a5c",
  "#e7ca9e",
  "#ffffff",
  "#f7b267",
];


export const STAGE_HEIGHT = {
  base: "38vh",
  md: "min(54vh, 540px)",
  xl: "min(58vh, 620px)",
};
export const STAGE_MAX_WIDTH = "min(94vw, 1500px)";

// the reveal shares the screen with a heading and a hint, so it sits smaller
export const REVEAL_HEIGHT = {
  base: "34vh",
  md: "min(48vh, 480px)",
  xl: "min(52vh, 540px)",
};

/** The idle mark is branding, not a product — hold it in off the stage edges. */
export const MYSTERY_SCALE = 0.65;
